import java.io.File;


public class Ffmpeg {
	
	/**
	 * Method to get file info and store it in the variable returned.
	 * @param file the file to be read
	 * @return the file info (command line output)
	 */
	public String getFileInfo(File file) {
		// RUN `ffmpeg -i
		return "";
	}
	
	/**
	 * Method to call FFmpeg to convert a video file
	 * @param src the file to convert
	 * @param dest the output file
	 * @return true if success on convertion, false otherwise.
	 */
	public boolean convertFile(File src, File dest) {
		//START TIMER
		
		/*
		 * Remember to push the converted Video Object into the queue.
		 */
		
		//END TIMER (RESULT IN MS)
		return true;
	}
}
